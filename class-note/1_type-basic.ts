const jsStr = "hello";

const tsStr: string = "hello";

// 숫자
const num: number = 10;

// 배열
const arr: Array<number> = [1, 2, 3];
const heroes: Array<string> = ["hell", "low", "duck"];
let items: number[] = [1, 2, 3];

// 튜플
let tupleArr: [string, number] = ["gangnam", 3];

// 객체
let obj: object = {};
let person: { name: string; age: number } = {
  name: "capt",
  age: 100,
};

// 진위값
let show: boolean = true;
